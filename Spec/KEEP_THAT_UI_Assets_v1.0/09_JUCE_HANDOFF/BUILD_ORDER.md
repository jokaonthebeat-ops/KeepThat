# KEEP THAT! UI Build Order

1. Create the top-level plugin editor at **1491 x 1055** and lock the aspect ratio.
2. Implement the static panel chassis using vector drawing, gradients, glows and shadows.
3. Add the header logo and preset navigation.
4. Implement the **Rolling Buffer HUD** with animated dual-color ring and time labels.
5. Add the **KEEP LAST** primary action button and two rows of capture-length selectors.
6. Build the **Live Input** panel with stereo meters, mini waveform and info labels.
7. Build the **Recovery Tools** stack with toggles and the Phrase Detected card.
8. Build the **Capture Preview** timeline with editable trim handles and time/bar ruler.
9. Add the left transport column and right action buttons.
10. Add the **Export / Destination** button grid.
11. Add the **Recent Keeps** scrolling tile browser.
12. Add the bottom macro control strip and horizontal stereo output meters.
13. Add hover, pressed, selected, disabled and focus states for all interactive controls.
14. Add placeholder animation data for all moving visuals.
15. Capture a screenshot at exactly **1491 x 1055** and compare it against the approved mockup.
16. Only after visual approval, connect the real rolling-buffer engine and capture/export workflow.
